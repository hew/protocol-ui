// Simple entry point for the ReScript CLI
// This just imports and runs our beautiful ReScript code

const { runComponentGenerator } = require('./ReScriptCLI.res.mjs');

// Run the CLI
runComponentGenerator();